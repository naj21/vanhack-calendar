```js
import Dropdown, { Option } from "../Dropdown";

<div style={{ display: "flex", justifyContent: "space-between" }}>
  <Dropdown theme={theme} text={"L"} position={"left"}>
    <Option theme={theme}>Driftwood sidecorp</Option>
    <Option theme={theme}>1 Position</Option>
  </Dropdown>
</div>;
```
